<script lang="ts">
	import Main from '$lib/components/Main.svelte'
	// import { getContext } from 'svelte';
	// let wi = $state(0);
	// let he = $state(0);
	// $effect(() => {
	// 	let { w, h } = getContext<{ w: number; h: number }>('clientSize');
	// 	wi = w;
	// 	he = h;
	// 	$inspect('main w and h: ', w, ' ', h);
	// });
	// $inspect('main w and h: ', wi, ' ', he);
</script>

<Main />
